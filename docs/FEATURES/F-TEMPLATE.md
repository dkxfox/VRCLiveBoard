# F-YYYYMMDD-NN 功能名字

> 复制本文件为 `F-<日期>-<序号>-<名字>.md` 后填写。**七问不答完不许进设计。**
> 流程见 `docs/PROCESS-02-开发更新.md`。

## 0. 状态

- 状态: DRAFT | DESIGN | BUILD | ACCEPT | DONE | DROPPED
- 归属: 插件(`plugins/<id>`) | 主体
- 目标版本: 1.4.0(milestone)/ 插件 x.y.z
- 关联: DEV-NOTES 条目号、commit

## 1. 七问(D0)

1. **谁在什么场景下用**:
2. **要不要占聊天框**:优先级 = ___,显示时长 = ___ 秒,与 ___ 冲突时 ___ 让位
3. **新配置项**:`config.___` = 默认值 ___(公开版默认**关闭**;若涉及安全,安全开关默认 **true**)
4. **新端点**:`GET/POST /api/___` → 响应 ___;错误码 ___;门禁级别 = 零级 / 一级 / 开发者级
5. **新依赖**:无 / `___`(体积 ___,能否 vendor 化 ___)
6. **失败降级**:主源失败 → ___ → 最终兜底 ___
7. **不做什么**:

## 2. 四张契约(D1)

### API 契约
| 端点 | 方法 | 请求 | 响应 | 错误码 | 门禁 |
| --- | --- | --- | --- | --- | --- |

### 配置契约
| key | 类型 | 默认值 | 说明 | 老配置缺键时 |
| --- | --- | --- | --- | --- |

### 显示契约
- 优先级: / ttl: / 字符预算: / 冲突让位:

### 权限契约(插件)
```json
{ "network": [], "filesystem": { "read": [], "write": [] }, "process": false, "ports": [] }
```

## 3. 切片计划(D2)

- [ ] 切片 1(可独立 revert):
- [ ] 切片 2:
- [ ] i18n 三语键:
- [ ] `config.default.json` 同步:

## 4. 验收断言(D3)

> 每条成功标准必须写成可执行断言,格式 `- ASSERT: 名字|/api/路径|正则`
> 跑法:`powershell -File scripts\checks\feature-accept.ps1 -Card docs\FEATURES\<本文件> -Port 19260`

- ASSERT: 示例-插件已加载|/api/plugins|示例插件id
- ASSERT: 
- [ ] 影响面回归(PROCESS-01 §6 矩阵)已勾选
- [ ] 用户在自己实例确认(UI/交互类必填)

## 5. 发布准备(D4)

- [ ] 使用说明.txt 章节
- [ ] 版本说明.txt 条目
- [ ] README 功能一览
- [ ] 打包清单($required / $xfFiles)决定
- [ ] 插件改版五件事(版本 / 重新授权提示 / 预置哈希 / 更新包 zip / 无需手工同步备份)
- [ ] `pack-audit.js` 通过
