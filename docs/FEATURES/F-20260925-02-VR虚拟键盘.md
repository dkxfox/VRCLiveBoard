# F-20260925-02 VR 虚拟键盘 / 打字面板(DRAFT · 可行性研究)

> 用户想法(2026-09-25): VR 状态下打字麻烦, VRChat 对中文输入法兼容不好; 想在世界里放个虚拟键盘展示在 VR 界面里(像 OVR Toolkit 的 Desktop Overlay)。
> 参考: 别人做的「VRCTT · VR 键盘 → 聊天内容框」(截图: 144 字计数 / 中文·英文切换 / 确定 / 历史 / 收起), **不公开获取方式**。
> 本卡目前只做 **可行性结论 + 分期建议**, 未进入设计/实现。

## 0. 状态

- 状态: **DRAFT(可行性已确认; 用户已补充"给所有人用 / SteamVR+VD / 走系统输入法 / 只要 VRC 聊天框 / 要语音输入"—— 细化研究见同目录 `F-20260925-02-VR虚拟键盘-技术研究.md`, 等用户拍板交付形态)
- 归属: 待定 —— 若走"键盘页"= 主体新增一个页面; 若走"自研 overlay"= 独立原生助手(不进主体)
- 关联: DEV-NOTES 216(本轮可行性结论)

## 1. 关键结论(先说答案)

1. **文本链路我们已经有了, 不需要动核心程序**: 主体已提供 `POST /v1/chatbox`(本地外部程序可直接推文本到聊天框, 与 /api/chatbox 同一处理器), 而聊天框本身走 OSC `/chatbox/input` —— 虚拟键盘只需要"产生文本 → 调这个 HTTP 接口"。
2. **真正缺的只有「在 VR 里显示 + 点按」这一层**, 而它有三种做法(按侵入性排序):
   - **A. 不自己画 overlay(零原生代码, 今天就能用)**: 把键盘做成主体里的一个页面(大按钮 + 中文输入), 用户在 VR 里用**已有的桌面视图**操作 —— SteamVR 自带 Desktop View、Virtual Desktop 的桌面、或用户已装的 OVR Toolkit / XSOverlay。
   - **B. 自研 SteamVR overlay(OpenVR IVROverlay)**: 截图那类工具的做法。需要 SteamVR 运行环境 + 一个**独立原生助手**(C++/C#; 项目里已有 C# launcher 先例)负责"画覆盖层 + 控制器射线当鼠标", 拿到的文本回传主体 HTTP 接口。**主体仍然零改动。**
   - **C. OpenXR overlay(XR_EXTX_overlay)**: 更"标准", 但生态成熟度与运行时覆盖不如 OpenVR(见参考资料), 建议按需再评估。
3. **中文输入有两条路**: ① 键盘页放一个隐藏输入框, 交给 **Windows 系统输入法**(最省事, 但候选窗在 overlay 里不好显示); ② **自带拼音候选**(JS 词库, 完全可控, 但要自己写候选/翻页/联想)—— 截图里那个「中文/英文」按钮说明对方多半走的是 ② 或自带 IME。
4. **144 字上限**与我们一致: VRChat 聊天框就是 144 字, 主体 composer 已有 maxChars=144 的处理, 键盘页做同一个计数器即可。

## 2. 七问(D0, 待用户确认的部分我标了?)

1. **谁在什么场景下用**: VR 里想打一段中文(不便开口/静音/需要留字)时, 不想摘下头显去用屏幕键盘。
2. **要不要占聊天框**: 不占。键盘只是**输入源**; 文本仍走聊天框的既有优先级(默认 80)与限频。
3. **新配置项**: 待定 —— 预计 `vrkeyboard.enabled`(默认 false, 遵守"新功能默认关闭")、`vrkeyboard.sendPriority`、`vrkeyboard.maxChars`(默认 144)、短语表 `vrkeyboard.phrases`、是否记录历史 `vrkeyboard.history`(默认 false, 与"不采集"口径一致)。
4. **新端点**: 复用 `POST /v1/chatbox`; 若要做"历史/短语/候选"再加 `GET/POST /api/vrkeyboard/*`(门禁级别待定, 建议零级但只本机可访问, 与既有 /v1/* 同口径)。
5. **新依赖**: A 方案 **零新增依赖**(纯前端页面); B 方案引入一个原生助手(OpenVR 头文件 + 构建链), **不进主体包**、按需单独分发, 主程序与插件目录都不受影响。
6. **失败降级**: 助手不在/SteamVR 没运行 → 键盘页仍可当普通网页用; 中文输入法不可用 → 退化为英文 + 短语; 发送失败 → 页面上给回执(不静默)。
7. **不做什么**: 不做自动发言/按脚本刷屏(只做人工输入); 不做"注入按键到别的程序"(那是 OVR Toolkit 的地盘, 风险与工作量都更大); 不做 Quest 独立机版本(需要完全不同的显示路径)。

## 3. 覆盖范围与风险(诚实)

- **SteamVR 是唯一"第三方 overlay 成熟"的路径**; Virtual Desktop / Quest Link / 独立机 情况各异(OVR Toolkit 这类工具本身就是跟着 SteamVR 走的, 参考其 Quest 讨论帖), 所以 B 方案只覆盖一部分用户 —— **需要先确认你自己的 VR 环境**。
- 主体侧风险低: 外部程序 + OSC 聊天框都是官方支持通道; 只要保持"人工输入"就不触碰 VRChat 的滥用红线。
- B 方案的真实成本不在"画键盘", 而在: 原生工程的构建/签名/更新链、以及"没装 SteamVR 时优雅降级"。

## 4. 建议分期(每期独立可交付)

| 期 | 内容 | 依赖 | 预估 |
| --- | --- | --- | --- |
| **P0** | 主体加「VR 打字」页: 大号键盘 + 输入框 + 144 计数 + 发送/撤销/空格/中英切换 + 常用短语 + 历史(可关); 用现有桌面视图即可在 VR 里操作 | 无(零新增依赖) | 0.5~1 天 |
| **P1** | 自研 SteamVR overlay 助手(最小可用: 字母/数字/常用符号 + 短语 + 发送 + 位置/大小记忆), 通过 `POST /v1/chatbox` 回传 | SteamVR + 原生助手 | 2~4 天 |
| **P2** | 中文候选(自带拼音) / 历史与短语管理 / 手势(抬手才显示、看键盘才亮) / 换肤 | — | 视需要 |

## 5. 参考资料

- OVR Toolkit(Desktop Overlay)商店页与其 Quest 讨论帖 —— 说明"第三方 overlay 跟 SteamVR 绑定"这件事;
- Khronos 论坛「Overlay app with OpenXR and Cpp」与 LunarG「Overlays in OpenXR」—— OpenXR overlay 的能力与现状;
- IwashiIME 文档 —— 同类"VR 里用输入法打字"的现成思路(日文);
- 用户提供的截图: VRCTT 键盘(144 计数 / 中英切换 / 短语 / 历史), 作为交互参考。

## 6. 待确认 / 下次开工从这里继续(2026-09-25 收尾)

**等用户确认(两件, 都只需看一眼 / 一句话)**
1. **LiveTranslate 的音频输入能不能选「麦克风」**(在它的设置里看一眼) —— 决定"复用 LiveTranslate 做语音输入"这条是否成立(它默认很可能是系统声音/回环);
2. **交付形态**: (a) 独立小工具(推荐, 主体体积不变、可独立更新) / (b) 随主程序出厂。

**第一步动作(不依赖上面的答案就能开工)**
- **P0-A(纯插件, 半天)**: 给 livetranslate 源加「语音输入模式」—— 读 _original.txt(**不读译文**, 否则中文说出去变英文上屏)、只在开启时推、默认关闭、日志/界面明确"正在听"; 做完即可在桌面上验证"说话 → 聊天框出字"的延迟与准确率;
- **P0-B(主体前端, 0.5~1 天)**: 「VR 打字」页(大键盘 + 144 计数 + 短语 + 中英切换 + 语音按钮占位), 用现有 OVR Toolkit / SteamVR 桌面视图即可在 VR 里操作。

**已定结论(下次直接引用, 不必重新调研)**
- 显示路径: **OpenVR IVROverlay(SteamVR)**; VD 串流推的就是 SteamVR 合成画面 → 覆盖层可用; 非 SteamVR 环境(纯 VD OpenXR / Quest Link / 独立机)需降级到"键盘页 + 已有桌面视图";
- 输入法: **走系统输入法**; 候选窗必须**自绘**(TSF 优先 + IMM32 兜底), 另有"区域截屏候选"与"只显示组合串"两种兼容模式可切换;
- 语音: 三条路(**LiveTranslate 复用** / Win+H 与输入法热键 / 自建 ASR), 结论是**分层**的, 不是单一方案;
- **主体零改动**: 文本统一走 POST /v1/chatbox(已是现成入口)。

**调研留档**: 同目录 F-20260925-02-VR虚拟键盘-技术研究.md(含 3.5 节"复用 LiveTranslate 的可行性")。
