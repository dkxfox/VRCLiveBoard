# 审计报告: 旧版 vs 新版控制台差异(2026-09-11, 子代理只读审计)

> 对照: 旧版控制台备份/(index.html、app.js、lang.js) 与 src/web/public/(index.html、app.js、app-security.js、lang.js)。
> 口径: 改名 / 有意去掉 / **移植丢失**(旧版有实际功能、新版界面不可达 = 用户用不到)。行号对应审计当时的快照(app.js 327 行)。
> 本文件是行动版; 本轮已修 1 条(showConsole 参数名), 其余挂账。

## 已修(2026-09-11)

| 问题 | 位置 | 修法 |
| --- | --- | --- |
| "显示命令行窗口"开关**反着坏**: 前端发 {show:...}, 服务端 server.js:804 只认 o.visible(visible!==false 恒真) → 取消勾选无效, 甚至把已关的窗口重新打开, 刷新后又变回勾选 | app.js:184 | 改为 {visible:this.checked} |

## 挂账(按用户可感知程度)

| 级别 | 问题 | 旧版依据 | 建议 |
| --- | --- | --- | --- |
| 高 | **新手引导整块丢失**(首访自动弹 + 不再自动 + localStorage vrcbGuideDone + 常驻按钮); 键 guideTitle/guideText/guideNoAuto/guideBtn/ok 都还在 lang.js | 旧H:397-410, 旧A:1156-1169 | 原样搬回新 UI(纯搬运) |
| 高 | 截图翻译**没有实时倒计时**: 旧版轮询 /api/status.ocrState 显示"倒计时: N...", 新版不读 ocrState(后端仍在) | 旧A:139-141 | pollStatus 里读 s.ocrState.phase === countdown 写进 #shotMsg |
| 高 | LiveTranslate **安装引导丢失**: 未检测到时旧版渲染 ltGuideHtml 三步图文, 新版只换一行字 | 旧A:1071-1076 | 把 ltGuideHtml 渲染到 #ltStatus 下方(键还在) |
| 高 | 插件管理退化三项: 删除按钮(后端 /api/plugins/remove 仍在) / 打开插件页面(hasPage → /plugin/<id>) / 刷新不再 POST /api/plugins/scan | 旧A:994,1012-1014,1040-1042 | 三处接回 |
| 中 | 控制台地址写死 http://127.0.0.1:19190/ (端口被占会自动 +1, 此时显示错地址); advHttpHint 一并丢失 | 旧H:88, 旧A:1205-1206 | 用 /api/ports 的 web.host/actual 动态填 |
| 中 | 数据源"说明"列不再显示运行状态/错误(lastError/helperRunning 后端仍返回) | 旧A:156-157 | 纯前端一行 |
| 中 | 健康总览弱化: /api/health 前端零调用(OSC 目标/Python/LT/插件统计看不到); healthCopy 退化成复制原始 JSON | 旧A:1107-1131,1136-1153 | 接回 /api/health 并格式化复制 |
| 中 | 顶部"发现新版本"按钮没了(只剩日志卡文字链, 且只在加载时查一次) | 旧H:91, 旧A:1088-1090 | 接回页头 + 6 小时轮询 |
| 中 | currentMeta("来源: xx · 优先级 n · 剩余 n 秒")丢失(后端仍给 sourceId/priority/ttlUntil) | 旧A:129-138 | 一行接回 |
| 中 | capInfo("自定义屏幕区域 x,y w×h"/未设置提示)丢失 | 旧A:317-325 | 接回 |
| 中 | portsInfo(当前实际监听端口 / OSC 目标 / 已热生效提示)丢失 | 旧A:1201-1215 | 接回(与地址动态化同源) |
| 低 | 插件每行优先级输入框丢失(只剩一键重置) | 旧A:976-987 | 按需 |
| 低 | 第三方插件的服务端渲染面板(/api/plugins/panel)前端零调用 | 旧A:997-1008 | 按需 |
| 低 | 13 段说明文字(transSysDesc/ocrDesc/visGuideLocal/envDesc/prioExplain/plgDesc/showConsoleDesc/advGameHint/advBoxHint/advHttpHint/diagHint/transVoiceDesc/ocrLtNote)整段没有新家 | 旧L 各键 | 按需补文案位置 |
| 低 | 保存/自启等成功无回执(旧版有"已保存 ✓(N 页)"/"已开启"等) | 旧A:115-116,254-255 | 走 note() 补一句 |
| 低 | 日志: 每次输入重新 fetch、不再跟随底部、错误正则收窄; 状态轮询 2s→5s | 旧A:215-248,1288 | 按需 |

## 已核实"改名/有意去掉"(不是丢失)

sidebar→#tabs、content→#layout、11 个 card-*→tab-*、current→curChat、diag→diagBtn、pages→bdList/edlist、rotMs→bdRot、savePages→bdSave、ltCheck→ltCheckBtn、ltOpen→ltDownloadBtn、transVoiceOn→transVoice、ocrtlMode→transMode、visBase/Key/Model→transApiBase/Key/Model、ocrtlBtn→btnShot、ocrtlOut→shotOut、capMode→transRegion、capAdj/Full→capAdjBtn/capFullBtn、srcs→srcRows、srcPrioReset→prioReset、plgRows→#plugCards、autoStart→advAuto、cfgExport/Import→expCfg/impCfg、portsCheckOut→portsOut。
有意去掉: navToggle(标签常显)、visionBox(输入框常显)、boardsCaret/boardsBody(改编辑区显隐)、thPlg/thVer/thPerm(表格→卡片)、consoleUrl 之外的若干轻量入口。
