# 审计报告: 桌面壳 + 打包/发布链路(2026-09-11, 子代理只读审计)

> 范围: electron/、src/main.js 的桌面壳交互、scripts/make-dist.ps1、scripts/checks/pack-audit.js、release-audit.ps1、gate-selftest.ps1、dev-dongle/ 与 devchain 相关入口。含对已存在产物(1.3.2 双 zip)的只读枚举。
> 本文件是行动版; 本轮已修 6 条(下表), 其余挂账。

## 已修(2026-09-11)

| 级别 | 问题 | 修法 |
| --- | --- | --- |
| **高(C1 级机密卫生)** | gate-selftest.ps1 复制整仓到 %TEMP% 做注入自测, 排除表**没有** dev-dongle / config.json(.bak) / 秘密开发* → 每次发布审计都把 dev-dongle/master/master.key、master-pass.txt、授权登记表.xlsx 与含 devchain+level1Password 的真实 config.json 复制到 %TEMP%(且只在脚本末尾删, 无 try/finally) | 排除表加 dev-dongle / 旧版控制台备份 / 测试|OCR|截图 目录 + /XF(config.json、config.json.bak、*.bak、秘密开发*、继续开发命令.txt、.gitignore、dev-unlocker*), 并用 config.default.json 顶一份干净的 config.json |
| 高 | 开发自测插件 plugins/conflict-test(全权限)会随包出厂, 而 pack-audit 又强制要求它在包里 | make-dist 插件复制循环跳过 conflict-test; pack-audit 官方插件枚举同样跳过 |
| 高 | pack-audit 禁入名单缺目录级规则: dev-dongle/(master.js 含 CHAIN_SALT 与整套 personSeed/PBKDF2)、母狗、chain.json、mini-dongle-、mini-unlock、.ocr-preview.png、.gitignore、startup-test | FORBIDDEN_NAME 补齐上述规则 |
| 中 | Electron setWindowOpenHandler 把**任意** details.url 交给 shell.openExternal(file:/ms-settings:/search-ms:/smb: 都可能被执行), 且无 will-navigate 守卫 | 只放行 https 才 openExternal, 其余一律 deny; 加 will-navigate 拦非本机导航 |
| 中 | .gitignore(逐条列出私有文件名 = 隐藏文件索引)、startup-test.html(启动动画测试台, 会被 19190 直接服务)、开发者文档/04-打包与分发.md(内部打包纪律)随包出厂 | $xfFiles 增加 .gitignore / startup-test.html / 打包与分发.md |

## 挂账

| 级别 | 问题 | 位置 | 备注 |
| --- | --- | --- | --- |
| 高 | make-dist.ps1 自称 ASCII only 却含 20+ 处裸中文, 全靠 BOM 撑着(审计期间实测曾无 BOM, PS5.1 解析 11 错) | make-dist.ps1 | G2 编码门禁已能拦住(本会话就拦过一次); 建议中文改码点构造 + release-audit 前置断言"打包脚本单 BOM + PARSE_OK" |
| 高 | 排除表是"防出厂的唯一执行者", 却没有机器化守卫 | make-dist.ps1 与 pack-audit.js 两套名单 | 建议提升为仓库清单文件由两边共用(需拍板格式) |
| 中 | 打包与审计无时序绑定: release-audit 不构建、只审计已存在的 zip; make-dist 只跑窄扩展名 Check-Stage(不含 .js/.md/.html); dist 公开版 旧包/旧 SHA256SUMS 会残留(make-dist:19 的清理过滤 dist 根 = 死代码) | release-audit.ps1:58-66, make-dist.ps1:19,87-99,167 | 建议: make-dist 末尾自动跑 pack-audit + 写 BUILD-INFO(git commit), release-audit 校验新鲜度; 清空 $pubDir; stage 清理放 try/finally |
| 中 | 首次运行从第三方镜像(npmmirror)下载 Electron 并直接解压执行, **无哈希校验** | scripts/install-electron.js:15-23 | 建议改官方 release + SHASUMS256 校验 |
| 中 | 插件更新包(dist/插件更新包/*.zip)完全不在审计范围, 而 PROCESS-03 写明 3B 应覆盖它 | release-audit.ps1:60 | 建议纳入 pack-audit(--plugin-pack 模式) |
| 中 | docs/ 与仓库根的出厂范围未拍板: 开发者文档/(5 个 .md)、docs/ISSUES.md、docs/PROCESS-*.md、docs/*-BASELINE.json、版本说明.txt(含"母狗/迷你狗"内部条目)、ClearIconCache.bat 都在 1.3.2 包里 | make-dist 排除表 | 建议 docs/ 改白名单式出厂(只发 DEV-NOTES/GLOSSARY/PLUGIN-DEV/LIVETRANSLATE/RESEARCH) |
| 中 | $xfFiles 裸名 config.json 会连带排除任意子目录同名文件 | make-dist.ps1:74-75 | 改相对路径精确处理 + pack-audit 断言"根之外不得有 config.json" |
| 低 | 发行版里保留调试开关(VRCB_USER_DATA 可重定向 userData 绕单实例锁、VRCB_HEADLESS_TEST、Ctrl+R) | electron/main.js:45,96-101,136 | 建议用 app.isPackaged 关掉 |
| 低 | 托盘创建失败被静默 catch, 而 close→hide 仍然生效 → 关窗后无窗口无托盘, 只能任务管理器 | electron/main.js:102,139-150 | Tray 失败时禁用 hide |
| 低 | 核心注册 vrcb:shutdown 监听在启动末尾, 启动过程中退出(可能已拉起 python/截图助手)会跳过清理 | electron/main.js:124 与 src/main.js:131 | 早注册一个清理钩子 |
| 低 | 母狗把每人一级密码打到 stdout | dev-dongle/master/master.js:353(及 :339) | 与 auth-state-check.js 自定"永不打印一级密码"冲突 |
| 低 | web.stop() 只 server.close, 不等连接排空(桌面壳 keep-alive 时每次退出固定等 1.5s 兜底) | src/web/server.js:1004 | 补 closeAllConnections?.() |
| 低 | 审计报告写入路径: release-audit.ps1:61 脚本块内 exit 1 会跳过报告写入 | release-audit.ps1:61 | 改置 $script:exit=1 + return |
| 低 | VRCB_ELECTRON_ZIP 指向任意本地 zip 也会被解压执行(本地攻击面) | install-electron.js:17 | 低 |

## 已核实无问题(不必重复排查)

1. 旧版控制台备份/ 与 秘密开发-彩蛋设计* 确实不在 1.3.2 双 zip 里(虚惊; 本轮又加了 pack-audit 规则)。
2. 测试素材目录(测试视频/测试OCR功能用截图)由动态目录名匹配排除, zip 内确认无。
3. 全仓无 IPC(ipcMain/ipcRenderer/preload/contextBridge 0 命中), 不存在 IPC 输入校验缺口。
4. 退出/重启链干净: 三个 interval、composer、media(python 助手)、截图常驻助手、web(1.5s 上限)、osc 全部有序收; 端口经 VRCB_CONSOLE_PORT + vrcb:console-ready 回传实际端口(含 EADDRINUSE 回退), 无写死端口白屏。
5. 无硬编码开发路径/端口(src/electron/scripts/launcher 内 Tank_os、C:\Users、Z:\ 全 0 命中)。
6. 出厂 config.json 脱敏链有效(default 覆盖 → Scrub-Secrets → Check-Stage → pack-audit 三条断言), 1.3.2 包内 config.json 只有默认值。
7. devgate/加密狗没带进发行代码: src/devgate.js 只有 SALT + 单向校验; master.js 的 make-mini/authorize 不复制 master.key/master-pass.txt; mini-unlock.ps1 不含密钥。
8. 发布产物内部自洽: SHA256SUMS-v1.3.2.txt 与双 zip 哈希一致; 文件名 UTF-8+EFS; 无 .bak。
