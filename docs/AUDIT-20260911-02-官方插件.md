# 审计报告: 4 个官方插件(2026-09-11, 子代理只读审计)

> 范围: plugins/ 下 5 个目录(friend-welcome / scheduled-board / weather-board / netease-lyrics / conflict-test), 只看自研代码, 不审 vendor 与 node_modules。
> 前提事实: 宿主是**契约式权限**而非沙箱 —— 插件若直接 require('fs')/require('child_process') 可绕过 ctx.* 的审计; manager.js 的 require 钩子默认只审计不拦。
> 本文件是行动版(逐条标状态), 结论均已由本人复核到文件行。

## 已修(2026-09-11, 见 M-20260911-27 / DEV-NOTES 143)

| 级别 | 问题 | 修法 |
| --- | --- | --- |
| 高 | netease-lyrics 的 manifest 声明 process:false, 实际却 spawn/execFileSync PowerShell、查注册表、taskkill /F 强杀网易云、spawn 任意 cloudExe —— 审批红字只看 permissions.process, 属**声明不实/告知失效** | manifest: process -> true; ports -> [9234]; description 明说"需要进程能力 + 占用调试端口 9234"。GPLUG 0 FAIL / 0 WARN |

## 挂账(未修)

| 级别 | 问题 | 位置 |
| --- | --- | --- |
| 中 | 未声明 fs 却写桌面 .lnk(且 fs 不在审计钩子内) | netease-lyrics/index.js:155-169 |
| 中 | 网络门禁绕过: 用全局 fetch/WebSocket, 且不校验 /json 返回的 webSocketDebuggerUrl 的 host; cdpPort 可被写成 "9234@evil.tld"(POST /api/plugins/config 无校验) | netease-lyrics/index.js:198-208, cdp.js:73-77 |
| 中 | _retryTimer 在 await 之后才赋值: 期间停用插件则该 interval 之后才创建且引用已丢 -> 停用后每 15s 仍重连 | netease-lyrics/cdp.js:68, 113-119 |
| 中 | _send 无超时 + _pending 无上限: ws 卡住时每秒新增永不 settle 的条目, dispose 也不 reject | netease-lyrics/cdp.js:90-112 |
| 中 | busy 一次异常即永久卡死: lines 非数组时 map 抛错被吞, .then 无 .catch -> 此后所有好友进房被静默丢弃 | friend-welcome/index.js:29-34 |
| 中 | 子串匹配(好友名填"小"会命中所有含该字的陌生人) + 未传 force(宿主默认 force=true, priority 90 抢占并绕限频) | friend-welcome/index.js:28-34 |
| 中 | 审批哈希只覆盖 index.js: cdp.js / bat / ps1 不在哈希内, 替换它们不会使授权失效 | pluginsys/manager.js:91-100 |
| 中 | 导入无上限串行 geocode(每行 15s 超时, /api/plugins/call 无超时) | weather-board/index.js:143-159 |
| 低 | 面板 HTML 是死代码(现役控制台只认 window.__plgset_<id>), 但含未转义拼接; 旧版控制台 app.js:998 用 innerHTML 渲染 -> 若恢复动态面板即存储型 XSS | scheduled-board:148/156, weather-board:233, netease:250/252 |
| 低 | status() 不返回 cfg, 而 app.js:312 读 j.cfg -> 设置面板回显永久失效 | netease-lyrics/index.js:136-138 |
| 低 | testFire 只认 hour/special, app.js:284 传 regular -> "立即测试播报"永远失败 | scheduled-board/index.js:122-133 |
| 低 | manifest.exclusive 为空却在 index.js:80 占用 chatbox-timeline(静态冲突检测看不见) | friend-welcome |
| 低 | 空 catch 吞异常(processPolicy=deny 时显示"未找到网易云安装"而非"策略禁止") | netease index.js:177/184/206, cdp.js:84/92 |
| 低 | spawn 未监听 'error'(路径不可执行时报"启动超时", 靠全局兜底才没崩) | netease index.js:172/208 |
| 低 | 入参未校验 Array: POST {rows:"abc"} 会把字符串逐字符当行并整表覆盖用户名单 | friend-welcome:74, scheduled-board:93, weather-board:144 |
| 低 | 一次性 setTimeout 未登记: 停用后 1.2/3 秒仍发一条 | weather-board:157/197 |
| 低 | conflict-test(开发自测, 全权限)随发行目录分发 | plugins/conflict-test |
| 低 | manifest.ai 写在 permissions 里而宿主读顶层 ai(口径不一致); friend-welcome 注释版本漂移 | conflict-test, friend-welcome/index.js:2 |

## 已核实无问题(不必重复排查)

1. friend-welcome / scheduled-board 的权限与实现完全一致。
2. weather-board 只请求声明的 3 个域名且全走 ctx.http(超时 + 缓存 + 降级 + 兜底)。
3. 未发现任意文件读写 / 路径遍历(仅固定桌面 .lnk); /api/plugins/asset 有 resolve + startsWith 防穿越。
4. 未发现命令注入(spawn/execFileSync 无 shell:true; bat/ps1 变量带引号、-LiteralPath)。
5. 未发现端口监听残留 / 临时文件残留; disable() 统一清 entry.timers, 四个插件 dispose 都停定时器。
6. 未发现能搞崩宿主的路径(全局兜底 + 回调隔离)。
7. 未发现敏感信息入日志(无密钥/UID/完整路径)。
8. 数值夹取到位; 无原型污染; firedKeys 增长可忽略。
